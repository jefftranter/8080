; Macros to emulate the Heathkit HDOS assembler.

; Code currently commented out as the assembler enforces that the
; expression must be evaluatable in first pass.

ERRNZ   MACRO EXP
        IF (EXP) != 0
          ERROR Expression is not zero
        ENDIF
        ENDM
